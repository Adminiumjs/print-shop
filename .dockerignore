node_modules
dist
.git
.github
.vercel
.DS_Store
*.local
npm-debug.log
# Full-stack self-host assets — not needed to build the static web image.
.env
.env.*
docker-compose.yml
db
# manifest.json is NOT excluded, though it looks like it belongs here: since
# wave 4 `src/manifest.test.ts` imports it, so `npm run build` (tsc -b && vite
# build) fails inside the image without it. The thirteen wave-3 apps can
# exclude it because none of them reads it from src/.
