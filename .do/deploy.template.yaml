spec:
  name: hotel-reservations
  static_sites:
    - name: web
      github:
        repo: Adminiumjs/hotel-reservations
        branch: main
        deploy_on_push: true
      build_command: npm run build
      output_dir: dist
      environment_slug: node-js
